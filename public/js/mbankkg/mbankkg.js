(function($){samo.mbankkg=function(){samo.acquiring.work('mbankkg');samo.acquiring_confirm_sms=function(){var ROOT_URL=samo.ROUTES.mbankkg.url;const $container=$('#acquiring_mbankkg_sms_container');const $form=$container.find('form');$form.find('.acquiring-submit-sms').on('click',function(e){e.preventDefault();let sms=$form.find('input#sms-code').val().trim();let inumber=$form.find('input#acqf-inumber').val();if(inumber!==""){var params={};params.SMS=sms;params.INUMBER=inumber;params.samo_action='SMS_CONFIRM';$.getScript(ROOT_URL+$.param(params));}});};};})(samo.jQuery);